# Render Blueprint for School Management System
# Reference: https://render.com/docs/blueprint-spec

databases:
  - name: school-management-db
    databaseName: school_management
    user: school_management_user
    plan: free
    ipAllowList: [] # required key even if empty

services:
  - type: web
    name: school-management-system
    env: go
    plan: free
    buildCommand: go mod download && go build -o school-management-system
    startCommand: ./school-management-system
    autoDeploy: true
    envVars:
      - key: GIN_MODE
        value: release
      - key: DATABASE_URL
        fromDatabase:
          name: school-management-db
          property: connectionString
      - key: PORT
        value: 8080
